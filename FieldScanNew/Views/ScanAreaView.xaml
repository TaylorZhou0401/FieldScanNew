<UserControl x:Class="FieldScanNew.Views.ScanAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FieldScanNew.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ScanAreaViewModel}">
    <StackPanel Margin="20">
        <GroupBox Header="扫描范围与点数" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="起点 (X, Y):" VerticalAlignment="Center" Margin="5"/>
                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                    <TextBox Text="{Binding Settings.StartX}" Width="80" Margin="5"/>
                    <TextBox Text="{Binding Settings.StartY}" Width="80" Margin="5"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="终点 (X, Y):" VerticalAlignment="Center" Margin="5"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                    <TextBox Text="{Binding Settings.StopX}" Width="80" Margin="5"/>
                    <TextBox Text="{Binding Settings.StopY}" Width="80" Margin="5"/>
                </StackPanel>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="点数 (X, Y):" VerticalAlignment="Center" Margin="5"/>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <TextBox Text="{Binding Settings.NumX}" Width="80" Margin="5"/>
                    <TextBox Text="{Binding Settings.NumY}" Width="80" Margin="5"/>
                </StackPanel>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="扫描高度 (Z):" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Settings.ScanHeightZ}" Width="80" Margin="5"/>
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>