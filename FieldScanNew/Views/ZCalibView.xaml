<UserControl x:Class="FieldScanNew.Views.ZCalibView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:FieldScanNew.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ZCalibViewModel}">
    <StackPanel Margin="20">
        <GroupBox Header="手动控制" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <TextBlock Text="步长 (mm):" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding StepSize}" Width="60" Margin="10,0,0,0"/>
                </StackPanel>
                <UniformGrid Columns="2" Rows="3" Margin="0,15,0,0">
                    <Button Content="X+" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="X+"/>
                    <Button Content="X-" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="X-"/>
                    <Button Content="Y+" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="Y+"/>
                    <Button Content="Y-" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="Y-"/>
                    <Button Content="Z+" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="Z+"/>
                    <Button Content="Z-" Margin="5" Command="{Binding MoveJogCommand}" CommandParameter="Z-"/>
                </UniformGrid>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>